{
  "name": "algohub-extension",
  "version": "1.0.0",
  "description": "AlgoHub Extension",
  "main": "dist/background.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "copyfiles -u 1 src/assets/**/* dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.299",
    "copyfiles": "^2.4.1",
    "typescript": "^5.7.3"
  }
}
